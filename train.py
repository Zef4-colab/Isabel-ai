import json\nimport os\nfrom sklearn.model_selection import train_test_split\nfrom sklearn.feature_extraction.text import CountVectorizer\nfrom sklearn.naive_bayes import MultinomialNB\n\n# Load data from the dialogo.json file\ndef load_data(file_path):\n    with open(file_path, 'r') as file:\n        data = json.load(file)\n    return data['messages']\n\n# Main training function\ndef train_isabel_model():\n    # Step 1: Load data\n    messages = load_data('dialogo.json')\n\n    # Step 2: Preprocess data\n    texts = [msg['text'] for msg in messages if 'text' in msg]  # Extract text from messages\n    labels = [msg['label'] for msg in messages if 'label' in msg]  # Extract labels\n\n    # Step 3: Split data into training and test sets\n    X_train, X_test, y_train, y_test = train_test_split(texts, labels, test_size=0.2, random_state=42)\n\n    # Step 4: Extract features using CountVectorizer\n    vectorizer = CountVectorizer()\n    X_train_vectorized = vectorizer.fit_transform(X_train)\n\n    # Step 5: Train the model\n    model = MultinomialNB()\n    model.fit(X_train_vectorized, y_train)\n\n    # Step 6: Evaluate the model\n    X_test_vectorized = vectorizer.transform(X_test)\n    accuracy = model.score(X_test_vectorized, y_test)\n    print(f'Accuracy: {accuracy * 100:.2f}%')\n\nif __name__ == '__main__':\n    train_isabel_model()